// Solidity version
pragma solidity ^0.5.16;

// Declares smart contract
contract TodoList {
  //Tracks amount of tasks in smart contract
  // State variable - written to the blockchain (like class variable)
  uint public taskCount = 0;
}
